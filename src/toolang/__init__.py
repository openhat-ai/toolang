"""Toolang runtime package.

This package contains the current Toolang agent runtime, shared plugin
boundary, CLI, and built-in runtime families. Stable subdomains are exposed
through focused packages such as `toolang.base`, `toolang.execution`,
`toolang.models`, and `toolang.tools`.
"""
