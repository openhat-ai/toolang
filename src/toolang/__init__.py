"""Toolang runtime package.

This package contains the local-first agent runtime, CLI, sync pipeline, and
runtime support packages. Stable subdomains are exposed through focused
packages such as `toolang.agent`, `toolang.runtime`, and `toolang.sync`.
"""
