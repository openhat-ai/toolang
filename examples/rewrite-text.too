#@ Rewrite a message using primary input and named arguments.

agic rewrite(_, audience, tone?):
  recall = none
  tools = none

  Rewrite the following text for {{audience}}.
  Use this tone when provided: {{tone}}. Otherwise use a neutral tone.
  Preserve facts, names, numbers, and commitments. Return only the rewrite.

  {{_}}
