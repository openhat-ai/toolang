#!/usr/bin/env toolang

# Manual check for simulating a short thread in one `toolang invoke` call.
# `recall = none` keeps stored thread history out of the model input, so the
# explicit user/assistant/user blocks below are the only conversation history.
#
#   toolang examples/invoke.simulated-history.none.too followup "What is my name?"

agic followup(_: Part[]):
  recall = none
  context none

  user:
    My name is Ada.

  assistant:
    Nice to meet you, Ada.

  user:
    Answer using the simulated conversation above: {{_}}
